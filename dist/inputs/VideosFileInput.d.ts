import { FormElements } from "../types";
import { FileInput } from "./FileInput";
export declare class VideosFileInput extends FileInput {
    constructor({ inputElement, errorMsgElement, }: FormElements["videos"]);
}
