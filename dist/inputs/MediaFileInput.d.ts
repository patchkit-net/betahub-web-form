import { FormElements } from "../types";
import { FileInput } from "./FileInput";
export declare class MediaFileInput extends FileInput {
    constructor({ inputElement, errorMsgElement, }: FormElements["media"]);
}
