import { FormElements } from "../types";
import { Input } from "./Input";
export declare class StepsToReproduceInput extends Input {
    constructor({ inputElement, errorMsgElement, }: FormElements["stepsToReproduce"]);
}
